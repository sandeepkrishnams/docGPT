version: "3.8"

services:
  postgres:
    image: postgres:latest
    container_name: postgress
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: bridgeUk3uasj
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgres-data:
    external: true
